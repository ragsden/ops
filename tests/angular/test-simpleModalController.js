describe('Testing simpleModalController',function() {
	var ctrlScope;
    var modalInstance;
	var ctrl;
/*	beforeEach(function() {
    	module('angSpa');
		inject(function($rootScope, $modalInstance, $controller) {
          ctrlScope = $rootScope.new();
          modalInstance = $modalInstance;
          ctrl = $controller('simpleModalController',
                 {
					$scope : ctrlScope, 
                    $modalInstance : modalInstance
		         });
        });
    });

	it('should check existence of scope variables when created', function() {
	});
*/
});
